---
title: "Uniac quickstart"
description: "This example starts with an application whose root Dockerfile builds a service listening on 0.0.0.0:8080. The machine has the Uniac CLI, a running Docker daemon, and account access. It uses a new project named my-app, supplies its application description directly, and selects the project through a directory binding."
---

{/* Generated from uniac-ai/agent-skills: skills/uniac-quickstart/SKILL.md */}

## Example application

This example starts with an application whose root `Dockerfile` builds a
service listening on `0.0.0.0:8080`. The machine has the Uniac CLI, a running
Docker daemon, and [account access](/cli/authentication).
It uses a new project named `my-app`, supplies its application description
directly, and selects the project through a directory binding.

The following `uniac.yaml` declares one service and exposes its HTTP port:

```yaml
runtime: yaml
default: main
resources:
  api:
    type: service
    build: .
  main:
    type: deployment
    services:
      api:
        from: api
        public_ports: [{ port: 8080, type: http }]
```

[Composition in YAML](/composition/yaml)
defines the relationship between `api`, its definition, and `main`.

## Destination and deployment

With that application and manifest in the current directory:

```sh
uniac plan
uniac project create my-app
uniac link my-app
uniac deploy
uniac status
```

An existing project can be linked by name or slug instead of creating one.
The [project-selection contract](/cli/overview#project-selection)
also describes selection during deployment and target overrides.

The [CLI reference](/cli/overview#planning-and-deployment)
defines these commands' requirements and completion behavior.

## Application address

The HTTP address in the service's `endpoint` row is this application's
public URL. [Output and errors](/cli/output)
describes the report; [Service](/resources/service)
defines what the observed state establishes.
