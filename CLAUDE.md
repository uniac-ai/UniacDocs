# UniacDocs

Documentation for **Uniac**. Don't document internal implementation; that belongs in UniacInfra.

When writing, apply the uniac skill's principles.

Mintlify mechanics — commands, components, voice — live in `AGENTS.md`.
